# 프로젝트 현황 및 가이드

이 문서는 포트폴리오 웹사이트 프로젝트의 현재 상태, 구현된 기능, 그리고 향후 개발 방향에 대한 가이드를 제공합니다.

## 1. 프로젝트 개요

- **목표:** 온라인으로 조회하고 PDF로 깔끔하게 인쇄할 수 있는 개인 포트폴리오 웹사이트 구축.
- **핵심 기능:**
  - 숨겨진 관리자 로그인 후, 웹페이지의 모든 텍스트 콘텐츠를 실시간으로 수정할 수 있는 '편집 모드' 기능.
  - 수정된 모든 내용은 데이터베이스에 영구적으로 저장되며, 웹사이트에 방문한 모든 사용자에게 **실시간으로 반영**되어야 함.
- **현재 상태:** **핵심 기능 구현 완료.** UI, 실시간 콘텐츠 편집, 데이터베이스 연동 및 포트폴리오 상세 페이지 기능까지 모두 구현되었습니다.

## 2. 기술 스택

- **언어:** TypeScript
- **프레임워크:** React (v19), React Router
- **빌드 도구:** Vite
- **스타일링:** Tailwind CSS
- **데이터베이스/백엔드:** Supabase (Auth, Realtime, DB)
- **코드 품질:** ESLint

## 3. 주요 기능 및 구현 현황

| 기능 | 상태 | 설명 |
| :--- | :--- | :--- |
| **UI (디자인)** | 🟢 **완료** | `Design.html`을 기준으로 모든 UI 컴포넌트의 스타일과 구조가 구현되었습니다. |
| **로그인 / 편집 모드** | 🟢 **완료** | `Ctrl`+`Alt`+`L` 단축키를 통한 관리자 로그인 및 편집 모드 활성화 기능이 구현되었습니다. |
| **콘텐츠 편집 기능** | 🟢 **완료** | 모든 섹션(`Header`, `기본정보`, `학력`, `자격증`, `경력`, `자기소개`, `포트폴리오`)에 대한 '클릭하여 수정' 및 항목 추가/삭제 기능이 구현되었습니다. |
| **데이터 영속성/실시간**| 🟢 **완료** | 모든 콘텐츠 변경사항은 Supabase DB에 영구 저장되며, 모든 접속자에게 실시간으로 동기화됩니다. |
| **이미지 직접 업로드** | 🟢 **완료** | 편집 모드에서 로컬 파일을 선택하여 프로필, 포트폴리오 이미지를 직접 업로드하고 교체하는 기능이 구현되었습니다. |
| **포트폴리오 상세 페이지**| 🟢 **완료** | 포트폴리오 목록의 각 항목을 클릭 시, 별도의 상세 페이지로 이동하는 기능 및 상세 페이지 내부 콘텐츠 편집 기능이 구현되었습니다. |
| **사용자 경험 (UX)** | 🟢 **완료** | 데이터가 없는 섹션은 숨김 처리되며, 데이터 로딩 시 스켈레톤 UI를 통해 콘텐츠 깜박임 현상을 방지합니다. |


## 4. 브랜치 전략 및 배포

- **브랜치 전략:**
  - `main`: 실제 서비스될 프로덕션 브랜치입니다.
  - `feat/{기능이름}`: 새로운 기능 개발 시 사용하는 브랜치입니다. 개발 완료 후 `main`으로 Pull Request를 보냅니다.
- **배포:**
  - `Vercel`을 통해 배포합니다.
  - `main` 브랜치에 코드가 병합(merge)되면 자동으로 배포가 진행됩니다.

## 5. 주요 작업 지침

### 환경 변수 (`.env.local`)

- 프로젝트 루트에 `.env.local` 파일이 필요합니다. Supabase 연동을 위한 URL과 키를 저장합니다.
- 이 파일은 보안을 위해 `.gitignore`에 등록되어 Git에 올라가지 않습니다.
  ```
  VITE_SUPABASE_URL="YOUR_SUPABASE_URL"
  VITE_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"
  ```

### 전역 상태 관리 (Context API)

- **인증/편집 모드:** `AuthContext` (`src/contexts/AuthContext.tsx`)가 관리합니다.
  - 컴포넌트 내에서 로그인 상태나 편집 모드 여부를 확인하려면 `useAuth()` 훅을 사용하세요.
- **알림 (Toast):** `ToastContext` (`src/contexts/ToastContext.tsx`)가 관리합니다.
  - 알림을 띄우려면 `useToast()` 훅을 사용하세요.

## 6. 아키텍처 및 다음 단계

'클릭하여 수정' 및 실시간 동기화 기능의 기반 아키텍처가 확립되었습니다. 모든 컴포넌트는 이 구조를 따라 일관성 있게 구현되었습니다.

### 핵심 아키텍처

1.  **범용 편집 컴포넌트 (`Editable.tsx`)**
    - '클릭하여 수정'에 필요한 모든 UI/UX 로직을 캡슐화한 텍스트 편집용 범용 컴포넌트입니다.

2.  **범용 이미지 업로드 컴포넌트 (`ImageUpload.tsx`)**
    - '클릭하여 업로드' 로직을 캡슐화한 이미지용 범용 컴포넌트입니다. Supabase Storage 연동, 업로드 상태 관리, 미리보기 기능을 내장하고 있습니다.

3.  **데이터 관리 커스텀 훅 (`useSectionData`, `useTextData`)**
    - 로딩 상태 관리, 데이터 fetch, 실시간 구독, 데이터 저장 로직을 모두 내장하여 컴포넌트의 복잡도를 크게 낮추고, 페이지 로드 시 발생하던 콘텐츠 깜박임 현상을 해결합니다.

4.  **컴포넌트 구현 방식**
    - 각 컴포넌트는 자신의 데이터 타입에 맞는 커스텀 훅을 호출하여 데이터를 관리합니다.
    - 훅이 제공하는 `loading` 상태를 이용해 스켈레톤 UI를 표시합니다.
    - `Editable`, `ImageUpload` 등 범용 컴포넌트를 활용하여 일관된 UI/UX로 화면을 렌더링합니다.

### 다음 단계: 완료 및 향후 개선

현재 초기 기획 단계의 모든 핵심 기능이 구현 완료되었습니다. 향후 아래와 같은 방향으로 프로젝트를 더 발전시킬 수 있습니다.

- **고급 스타일링:** `@tailwindcss/typography` 플러그인을 도입하여 포트폴리오 상세 설명 등 긴 글의 가독성을 향상시킬 수 있습니다.
- **이미지 갤러리 구현:** 포트폴리오 상세 페이지 내에 여러 이미지를 업로드하고 표시하는 갤러리 기능을 추가할 수 있습니다.
- **테스트 코드 작성:** `Vitest`, `React Testing Library` 등을 사용해 컴포넌트 및 커스텀 훅의 안정성을 보장하는 테스트 코드를 작성할 수 있습니다.
- **상태 관리 고도화:** 향후 애플리케이션이 더 복잡해질 경우, `Zustand`와 같은 경량 상태 관리 라이브러리 도입을 고려할 수 있습니다.
